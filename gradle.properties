org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true
org.gradle.configuration-cache=false

loom_version=1.16-SNAPSHOT

# Minecraft versions EMUtils builds, tests and releases, oldest first. Each needs a target in build.gradle
# and a src/<version> folder (for example src/26_2).
supported_mc_versions=26.2

mod_version=0.13.70
moulconfig_version=4.6.0
kotlin_version=2.1.10
maven_group=net.emutils

emhelpers_version=0.1.4
