pluginManagement {
	repositories {
		maven {
			name = 'Fabric'
			url = 'https://maven.fabricmc.net/'
		}
		mavenCentral()
		gradlePluginPortal()
	}
}

rootProject.name = 'emutils'
def emHelpersBuild = file('../EMHelpers')
if (emHelpersBuild.isDirectory()) {
	includeBuild(emHelpersBuild)
}
